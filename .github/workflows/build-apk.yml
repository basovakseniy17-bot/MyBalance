name: Build APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Download project
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Extract Android project
        run: |
          mkdir project
          unzip -q MyBalanceAndroidProject.zip -d project
          echo "Files:"
          find project -maxdepth 3 -type f

      - name: Build APK
        run: |
          cd project/MyBalanceAndroid
          gradle assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: MyBalance-APK
          path: project/MyBalanceAndroid/app/build/outputs/apk/debug/app-debug.apk
